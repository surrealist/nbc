@model NBC.Models.SVUnitYear

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>ต้องการลบข้อมูลนี้ใช่หรือไม่?</h3>
    <div>       
        <dl class="dl-horizontal">
            <dt>
                @Html.Label("ปีงบประมาณ")
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.Year.Id)
            </dd>
    
            <dt>
                @Html.Label("หน่วย กสอ")
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.SV.Name)
            </dd>
    
            <dt>
                @Html.Label("หน่วยร่วม")
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.Unit.Name)
            </dd>
    
            
    
        </dl>
        @using (Html.BeginForm("Delete", "SVUnitYears", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <input type="submit" value="ต้องการลบข้อมูล" class="btn btn-primary btn-xs" /> |
                @Html.ActionLink("ย้อนกลับ", "Index", "SVUnitYears", null, new { @class = "btn btn-primary btn-xs" })
            </div>
        }
    
        @*@using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default" /> |
                @Html.ActionLink("Back to List", "Index")
            </div>
        }*@
    </div>
</body>
</html>
